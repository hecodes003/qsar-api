services:
  - type: web
    name: qsar-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn qsar_api:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
